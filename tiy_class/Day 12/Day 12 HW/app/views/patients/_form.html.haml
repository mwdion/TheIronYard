= form_for [@patient, @hospital] do |f|
  = if f.object.errors.messages.length > 0
    <!-- Render the errors to the user -->
    = f.object.errors.messages.inspect  
    %br
      -f.object.errors.messages.each do |key, value| 
        -value.each do |message| 
          = key = message 
          %br
  %p= f.text_field :first_name, class: 'form-control focusedInput', placeholder: 'Add First Name Here...'
  %p= f.text_field :last_name, class: 'form-control focusedInput', placeholder: 'Add Last Name Here...'
  %h4= f.label "Male"
  %p= f.radio_button :gender, "Male"
  %h4= f.label "Female"
  %p= f.radio_button :gender, "Female"
  %p= f.label :DOB, "Date of Birth"
  %p= f.date_select( :DOB, :start_year => 1900)
  %p= f.text_area :description, class: 'form-control ', placeholder: 'Description...', rows: '3'
  %p= f.submit "Add Patient", class: 'btn btn-success btn-block'
